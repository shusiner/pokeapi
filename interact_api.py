# This file is to interact with with pokeapi.co
import requests

from pokemon import Pokemon

def search_api(id_or_name: str) -> dict:

    pass

def serialize_api_result(result: str) -> dict:
    pass
# We can update db in two ways:
# 1. insert the record at the back
# 2. insert follow the string index
# For now we will use method one

def update_db(info: dict) -> None:
    pass

